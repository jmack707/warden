# secrets & generated material — never commit
.env
certs/**/*.key
certs/**/*.csr
certs/**/*.srl
certs/**/*.ext
certs/**/*.p12
certs/san.cnf
clients/*.key
clients/*.csr
clients/*.ext
clients/*.p12
# regenerated on every deploy — tracking them makes 'git pull' conflict on deploy hosts
clients/*.crt
certs/ca.crt
certs/ldap.crt
*.log
initdb.sql
openbao/.openbao-keys.json
